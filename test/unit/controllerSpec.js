

describe('Chat Controller', function() {

    describe('ChatController', function() {
        it ('should have a couple of chats in it', function() {
            var scope = {}, ctrl = new ChatController(scope);

            expect(scope.chats.length).toBe(2);
        })
        it ('click on fight should add a message to the top of the list', function() {
            var scope = {}, ctrl = new ChatController(scope);

            expect()
        })
    })
})
